
const double portraitItemExtent = 500;
const double landscapeItemExtent = 700;

enum PostType {
  photo,
  video,
  collection,
}

enum CarouselType {
  fullscreen,
  indicator,
}

enum MediaType {
  image,
  video,
}